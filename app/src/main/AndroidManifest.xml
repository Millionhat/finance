<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ia2.datagather.finanzas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finanzas">
        <activity android:name=".Activity.ReporteEspecifico"></activity>
        <activity android:name=".Activity.ReporteGeneralActivity" />
        <activity android:name=".Activity.AgregarGasto" />
        <activity android:name=".Activity.AgregarTarjeta" />
        <activity android:name=".Activity.DetalleTarjeta" />
        <activity android:name=".Activity.CardsActivity" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>